using System;

class Program
{
    static void Main(string[] args)
    {
        string[] lista = { "A", "B", "C", "D", "E" };

        foreach (String cont in lista)
        {
            Console.WriteLine(cont);
        }
    }
}
